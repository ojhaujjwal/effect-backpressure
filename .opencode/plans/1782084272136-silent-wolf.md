# Plan: Rename Rules + Add Config Presets

## 1. Rename Rules

### `pipe-max-arguments` → `no-pipe-overload`

| Action | Path |
|--------|------|
| Rename source | `src/oxlint/rules/pipe-max-arguments.ts` → `src/oxlint/rules/no-pipe-overload.ts` |
| Rename test | `test/pipe-max-arguments.test.ts` → `test/no-pipe-overload.test.ts` |
| Update import in entry | `src/oxlint/index.ts` — import path + rule registration key |
| Update `oxlintrc.json` | key `pipe-max-arguments` → `no-pipe-overload` |
| Update README | rule table entry |

### `no-disable-validation` → `no-schema-disable-validation`

| Action | Path |
|--------|------|
| Rename source | `src/oxlint/rules/no-disable-validation.ts` → `src/oxlint/rules/no-schema-disable-validation.ts` |
| Rename test | `test/no-disable-validation.test.ts` → `test/no-schema-disable-validation.test.ts` |
| Update import in entry | `src/oxlint/index.ts` — import path + rule registration key |
| Update `oxlintrc.json` | key `no-disable-validation` → `no-schema-disable-validation` |
| Update README | rule table entry |

## 2. Config Presets

Create `configs/` directory:

### `configs/recommended.json`
```json
{
  "rules": {
    "effect-lint/no-effect-ignore": "error",
    "effect-lint/no-effect-catchallcause": "error",
    "effect-lint/no-effect-asvoid": "error",
    "effect-lint/no-silent-error-swallow": "error",
    "effect-lint/no-service-option": "error",
    "effect-lint/no-nested-layer-provide": "error",
    "effect-lint/no-pipe-overload": "error",
    "effect-lint/no-schema-disable-validation": "error",
    "effect-lint/no-void-expression": "error",
    "effect-lint/no-oxlint-disable": "error",
    "effect-lint/no-process": "error",
    "effect-lint/no-effect-any": "error",
    "effect-lint/no-async-await": "error",
    "effect-lint/no-satisfies": "error",
    "effect-lint/no-raw-date": "error",
    "effect-lint/no-node-imports": "error",
    "effect-lint/no-unknown-effect-error": "error",
    "effect-lint/no-vitest-modifiers": "error",
    "effect-lint/no-ts-ignore": "error",
    "effect-lint/no-ts-nocheck": "error",
    "effect-lint/no-bun-globals": "error",
    "effect-lint/no-require": "error",
    "effect-lint/no-plain-it-with-effect": "error"
  }
}
```
23 rules. Excludes `prefer-option-from-nullable` and `no-console`.

### `configs/strict.json`
Extends `recommended` and adds the two remaining rules:
```json
{
  "extends": ["@your-org/oxlint-rules/configs/recommended.json"],
  "rules": {
    "effect-lint/no-console": "error",
    "effect-lint/prefer-option-from-nullable": "error"
  }
}
```

## 3. Update `package.json` Exports

Add to both `exports` and `publishConfig.exports`:
```json
"./configs/recommended.json": "./configs/recommended.json",
"./configs/strict.json": "./configs/strict.json"
```

Add `"configs/**/*.json"` to `"files"`.

## 4. Update `oxlintrc.json`

Keep as the full base config with all 25 rules, but update the renamed rule keys.

## 5. Update README

Add presets section:
```md
## Presets

### recommended (`configs/recommended.json`)
23 rules at "error" — covers Effect API safety, code quality, banned APIs, and test/comment practices.
Excludes `no-console` and `prefer-option-from-nullable`.

### strict (`configs/strict.json`)
Extends `recommended` and adds `no-console` and `prefer-option-from-nullable` at "error".
```

Update rule table with renamed rules and add a badge column showing which preset each rule belongs to.

## Verification

1. `pnpm test` — all tests pass (2 renamed, 0 broken)
2. `pnpm build` — compiles cleanly, dist contains renamed files
3. `cat configs/recommended.json` — verify 23 rules, no console/prefer-option
4. `cat configs/strict.json` — verify extends + 2 extra rules
